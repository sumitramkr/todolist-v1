<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Todo List</title>
  </head>
  <body>
    <% if (dayHTML === "Sunday" || dayHTML === "Saturday") { %>
    <h1 style="color: red">Today is <%= dayHTML %>.</h1>
    <% } else { %>
    <h1 style="color: blue">Today is <%= dayHTML %>.</h1>
    <% } %>
  </body>
</html>
